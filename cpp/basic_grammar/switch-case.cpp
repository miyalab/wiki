switch(pattern){
  case 0:
    // pattern == 0 のときに実行される処理;
    break;
    
  case 1:
  case 2:
    // pattern == 1 もしくは pattern == 2のときに実行される処理
    break;
    
  default:
    // patternがどのcaseにも合致しなかった場合に実行される処理
    break;
}
